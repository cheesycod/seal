<!-- markdownlint-disable MD033 -->
<!-- markdownlint-disable MD024 -->

# serde.gzip

`local gzip = require("@std/serde/gzip")`

---

### gzip.compress

<h4>

```luau
function gzip.compress(content: string | buffer, level: number?) -> buffer,
```

</h4>

Compresses gzip-encoded (.gz) data at compression `level`.

gzip levels range from 0 (fastest) to 9 (most compressed); defaults to 6.

---

### gzip.decompress

<h4>

```luau
function gzip.decompress(content: buffer, max_size: FileSize?) -> buffer,
```

</h4>

Decompress gzip-encoded (.gz) data up to `max_size` (default 1 GB).

---

Autogenerated from [std/serde/gzip.luau](/.seal/typedefs/std/serde/gzip.luau).

*seal* is best experienced with inline, in-editor documentation. Please see the linked typedefs file if this documentation is confusing, too verbose, or inaccurate.
